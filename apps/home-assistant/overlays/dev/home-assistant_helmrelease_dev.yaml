---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: home-assistant
  namespace: flux-system
spec:
  chart:
    spec:
      version: 0.3.x
  values:
    image:
      tag: "2025.9"
    controller:
      type: Deployment
    persistence:
      enabled: true
      existingVolume: home-assistant-pvc
